{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "gcc",
      "args": [
        "-g",
        "${workspaceFolder}/src/*.c",
        "-o",
        "${workspaceFolder}/build/satellite"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
